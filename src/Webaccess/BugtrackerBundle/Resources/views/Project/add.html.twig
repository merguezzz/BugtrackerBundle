{% extends "WebaccessBugtrackerBundle::layout.html.twig" %}

{% block title %}{{ parent() }}{% endblock %}

{% block description %}{{ parent() }}{% endblock %}

{% block bugtracker_body %}

	{% render "WebaccessBugtrackerBundle:Dashboard:menu" with {'route' : 'project'} %}

	<div id="dashboard" class="main-wrapper">
		<div class="header">
			<div class="title">
				<h1>{{ 'project.add' | trans }}</h1>
			</div>
			<div class="clear"></div>
		</div>

		<form action="{{ path('webaccess_bugtracker_project_add') }}" method="post" {{ form_enctype(form) }}>
		    {{ form_row(form.name, { 'label' : 'project.name', 'attr': {'class': 'span6'} }) }}
	        {{ form_row(form.company, { 'label' : 'project.company', 'attr': {'class': 'span6'} }) }}
	        {{ form_rest(form) }}
	        <a href="{{ path('webaccess_bugtracker_project') }}" class="btn btn-warning">Go back</a>
	        <input type="submit" class="btn btn-warning" value="Save" />
		</form>
	</div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}
